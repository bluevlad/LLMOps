# --- Web framework ---
fastapi==0.115.0
uvicorn[standard]==0.32.0

# --- Settings ---
pydantic==2.9.2
pydantic-settings==2.5.2

# --- Database (Phase 1b 활성화) ---
sqlalchemy[asyncio]==2.0.36
asyncpg==0.30.0
alembic==1.13.3

# --- Auth (Phase 1b 활성화) ---
python-jose[cryptography]==3.3.0
google-auth==2.35.0
requests==2.32.3              # google.auth.transport.requests 의존

# --- HTTP client (Ollama 폴링용) ---
httpx==0.27.2

# --- Scheduler (Phase 1b: Ollama/MLX 폴링 잡) ---
apscheduler==3.10.4

# --- Phase 2 (γ) 비교 실험 CLI (backend/scripts/run_comparison.py) ---
pyyaml==6.0.2
anthropic==0.39.0
openai==1.54.0

# --- Test ---
pytest==8.3.3
pytest-asyncio==0.24.0
respx==0.23.1                 # httpx 모킹
